<!doctype html>
<title>Karyn Reader's Website</title>
<link rel=stylesheet type=text/css href="{{ url_for('static', filename='css/view.css') }}">
  <div id="navbar">
    <h1>Karyn Reader, MSW, LCSW</h1>
    <ul id="nav">
      {% for id, caption in [('home', 'Home'), ('qualifications', 'Qualifications'), ('approach', 'Approach'), ('fees', 'Fees'), ('contact', 'Contact')] %}
        <li><a href="{{ url_for(id) }}" {% if id == active_page %} class="active"{% endif %}>{{caption}}</a></li>
      {% endfor %}
</ul>

{% block background %}
{% endblock background %}
</div>

  {% block page_content %}
  {% endblock page_content %}
